{
  "rules": {
    "fleet": {
      ".read": "auth != null",
      ".write": "auth != null",
      "$collection": {
        "$record": {
          ".validate": "newData.hasChild('_id')"
        }
      }
    }
  }
}
